{
  "name": "speedscore-book",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon --exec node -r dotenv/config server.js",
    "start": "node -r dotenv/config server.js",
    "test": "npx testcafe chrome tests/*.js",
    "b": "cd client && npm run build && cd .. && npm run dev",
    "install-all-dependencies": "npm install -g testcafe && npm i testcafe --save-dev && npm install passport-google-oauth20 && npm install passport-local && npm install bcrypt && npm install mongoose && npm install passport-local && npm install passport && npm install passport-github && npm install dotenv && cd client && npm install && npm install --save @fortawesome/free-brands-svg-icons && npm install bootstrap-social && npm run build && cd .. && npm i && npm run dev"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FSWebDevBook/speedscore-book.git"
  },
  "keywords": [
    "heroku",
    "react",
    "express",
    "node"
  ],
  "author": "C. Hundhausen",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/FSWebDevBook/speedscore-book/issues"
  },
  "homepage": "https://github.com/FSWebDevBook/speedscore-book#readme",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-session": "^1.17.2",
    "mongoose": "^6.0.13",
    "passport": "^0.5.0",
    "passport-github": "^1.1.0",
    "passport-google-oauth20": "^2.0.0",
    "passport-local": "^1.0.0",
    "path": "^0.12.7",
    "url": "^0.11.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.15.7",
    "@babel/core": "^7.15.8",
    "@babel/node": "^7.15.8",
    "@babel/preset-env": "^7.15.8",
    "@testing-library/react": "^12.1.2",

    "nodemon": "^2.0.14",
    "testcafe": "^1.17.1"
  },
  "engines": {
    "node": "14.x"
  },
  "type": "module"
}
